<div class="row">
  <!-- Entry key key -->
    <div id="{{ seminar.id }}" class="col-sm-10"> 
      <!-- Title -->
      <div class="title">{{ seminar.naslov }}</div>
    {{ seminar.content }}
    </div>  
</div>
